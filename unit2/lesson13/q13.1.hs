{-

If you ran the :info examples, you likely noticed that the type Word has come up a few times. 
Without looking at external resources, use :info to explore Word and the relevant type classes
to come up with your own explanation for the Word type. How is it different from Int? 

-}

-- `Word` is like `Int` but with different bounds
-- `Word` is essentially an unsigned int ie. positive values only
